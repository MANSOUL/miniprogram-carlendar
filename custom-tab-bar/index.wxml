<view class="tab">
  <view class="tab__item" wx:for="{{config.list}}" wx:key="{{index}}" data-index="{{index}}" bindtap="handleTap">
    <image class="tab__image" data-index="{{index}}" src="{{tabIndex === index ? item.selectedIconPath : item.iconPath}}"/>
  </view>
</view>